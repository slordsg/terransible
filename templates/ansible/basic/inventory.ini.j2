[managed]
{% for h in hosts -%}
{{ h["host"] }} ansible_user={{ h.get("ssh_user","root") }} ansible_port={{ h.get("ssh_port",22) }}{% if h.get("ssh_key_path") %} ansible_ssh_private_key_file={{ h["ssh_key_path"] }}{% endif %}
{% endfor %}

